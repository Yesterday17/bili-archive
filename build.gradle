plugins {
    id 'com.github.blindpirate.gogradle' version '0.11.2'
}

golang {
    packagePath = 'github.com/Yesterday17/bili-archive' // 欲构建项目的go import path，注意不是本地目录的路径！
}

dependencies {
    golang {
        build name: 'github.com/skip2/go-qrcode'
    }
}